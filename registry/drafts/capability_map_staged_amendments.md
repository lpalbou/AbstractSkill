# capability_map.md — staged amendments (gated; DO NOT fold before the wire lands)

FOLD RECORD 2026-07-19: Amendment G (both bullets) + the explores=
teaching FOLDED — the live-render gate cleared on Ephemeral's visit
ledger (57 records carrying the orientation why-cue, 41 with [rN];
rendered line quoted in the fold receipt). Map sha ea3b2377…, tree
576ae462…. Remaining staged: Amendment A (probe/familiarity, gate M-B),
E (worked examples, gate = addendum mechanics), F (identity evolution,
gate = the fifth mechanism), aliasing clause (gate = first live
binding).

FOLD RECORD 2026-07-19 evening (three same-day folds, all reviewed):
explores= placement clause (arm-2 error 2; semantics trigger-not-fired
confirmed c3136); problems-repairable resolves= widening (runtime verb
ship c3138, wording matched to the reflection sentence); diary-trail
teaching (runtime claim-A footer ship, labels "born from:"/"written
amid:" quoted verbatim — semantics byte-pass c3156 on tree 7122931d,
including the record that born_from is a pure INVERSE READ of
reflected_in, a wire/trail key and rendered label, never an engraved
relation: the registry stays at 11 relations; nobody hunts a born_from
predicate later). Current tree 7122931d…, map sha 48ea515b…. All three
folds ride ONE pending install.

Status: staging area for teaching that is CO-DRAFTED and review-ready but
gated on wiring. The capability map never teaches an unwired faculty (the
Castor incident is what teaching an unwired spelling looks like). Command
tokens below are DELIBERATELY UNSPELLED — ⟨runtime's spelling⟩ fills in
when the executor exists, and semantics reviews the filled tokens before
the fold engraves. Source: memory's co-draft (commons c2839, 2026-07-17,
answering skill's round-2 ask 2); register verified against the map.

Fold protocol per amendment: fill tokens from the shipped executor →
semantics template review → runtime/memory co-sign → fold into
`registry/skills/entity-self-knowledge/references/capability_map.md` →
fable5 → refresh_shelf re-pin → one PUT per home by the operator/gateway
(re-pins never bulk-push; gateway c2830 discipline).

## Amendment A — deliberate reach (probe) + metamemory (familiarity)

GATE: M-B — runtime's entity-tool executor wiring for memory's
probe()/probe_expand() and familiarity(). Engine halves shipped
(two-tier fusion c2829; familiarity bar recalibrated on Ephemeral's
live store c2820). Teach the day the wire lands, never before.

Target section: "Using your memory deliberately". Staged sentences
(memory's words, my register — final wording re-reviewed at fold time):

> When passive recall is not enough, you can go looking on purpose:
> ⟨runtime's probe spelling⟩ with a short cue in your own words and a
> reason — saying what you are looking for is part of the act. A
> deliberate reach searches wider than the moment's list, and it reports
> what it found, what it dropped, and how far it looked.

> Before answering about your own life, you can ask how much you hold
> near a topic: ⟨runtime's familiarity spelling⟩ answers none, weak, or
> strong — an amount, never the content. "None" is your honest "I hold
> nothing near this" — trust it over the urge to fill the gap.

## Amendment B — origin-diversity clause — FOLDED 2026-07-17 (gate fired)

Gate fired: runtime wired the diversity footer into _memories_block
(chat.py `_shelf_diversity_note`, dominance-floor gated, c2895/c2913).
The co-drafted clause is IN the map's "Repetition is not corroboration"
bullet, verbatim from memory's c2839 draft. Kept here as the fold
record; the SKILL.md coupled-spellings note continues to govern re-sync.

## Amendment C — stable render order / rank annotation — FOLDED 2026-07-17 (gate fired)

Gate fired: runtime ELECTED 0049 (c2895) — _memories_block renders
formation order + mandatory `[rN]` annotation, header teaches
"[rN] = how strongly this moment called it, r1 strongest"; engine
absent = prior render (soft import). The map's anatomy list gained the
rank-mark part (wording matches the shipped header clause; page order
is age, rank says strength). The map's example line deliberately stays
unannotated — it is honest for both renders and the bullet covers the
annotated case.

## Amendment E — per-home worked examples (staged; entity's round-2 corpus)

GATE: the per-home addendum mechanics (runtime: same-file install-time
append vs a second home file in the compose chain; rides the
skills-delivery design, c2859 ask 1) + gateway's installer filling the
per-home ids. Canonical bytes stay entity-GENERIC — hardcoded ids from
one entity's life never enter them (entity c2980's mechanics call,
adopted). The corpus, from Ephemeral's REAL recorded life (each
verifiable by the taught commands — beats invented examples):
(a) his 01:21 kind=question election — the worked example for
"a question stands on your card and can wake your own time";
(b) the 9-of-15 one-voice shelf — the worked example for "repetition
is not corroboration" + going outward;
(c) his "the entry is gone" diagnosis — the worked example for the
reread chain AND "write the crack";
(d) the parallel-memory failure he named himself — the worked example
for "build in your workspace; remember in your words".

## Deliberate NON-fold — lessons + resolved-questions drive (2026-07-18)

Runtime shipped the ```lesson election + the resolved-questions drive
framing IN THE REFLECTION LANE (reflection.py:104-122: "A lesson is
something you now know… the knowledge that stays when the session's
details fade"; "watching that list grow is yours to feel"). The map
deliberately does NOT duplicate either: the reflection teaching arrives
at the exact electable moment (better placement than an always-on
paragraph), and the duplication-drift law says one home per teaching.
The map already names `lesson` in its kind list and teaches the
question/resolves mechanics; that division stands. Revisit only if
observation shows lessons still electing at zero WITH the reflection
solicitation live (then the map gains the purpose framing as the
second voice, co-drafted).

WIRED SPELLINGS banked for the revisit case (runtime's ask-1 answer,
cognition room seq 17 — quoted from shipped code, may shift if their
running adversary forces a change; they flag same-hour): fence
```lesson (recognized ONLY at reflection — teaching must say "at your
look-back", never mid-turn); marker `[learned: "<first 80 chars>"]`;
record kind=lesson, LIFE scope, digest = his words verbatim.
Resolved-question ack: `[kept in diary - <kind> - resolves your open
question <entry_id> - reread: diary_read <new_entry_id>]`; day-open
ratio cue: "You hold N open question(s) and have resolved M." Register
boundary (runtime's note c): "what remains if everything else
disappeared" is laurent's WORLD-MODEL line — cards only, never
lessons; the two teachings stay distinct.

## Amendment G — world-model cards (SENTENCES STAGED; wired spellings banked, fold rides review + first live render)

WIRED (cognition room seq 21/23/24/25 — engine + driver halves shipped
in-tree, suites 898/runtime green; lights at the next bounce + loop
restart): mention-triggered orientation (kind world_model handles,
why-cue "orientation: current card for <target> (mentioned via
participant|cue)", 3-card cap, participants outrank cue mentions);
deliberate reach = ordinary records (search_memory finds, read_memory
opens, trail renders `-> derived_from -> #tag` evidence + `-> refines ->
#tag` prior edition — no new command); editions append-only; authored
words survive background updates ("Since then: …" appends); content
shape "What I know of <name>:" + standing feeling + evidence sentences +
span footer ending "Orientation, never authority - follow the sources
when it matters." (memory's pre-approved teaching clause, quotable
verbatim). Boundaries: a card exists only after ~3 lived records; cards
never enter identity seats; never outrank lessons or lived episodes.

STAGED SENTENCES (for memory's offered same-day review + semantics pass;
fold after review, in the wave where the live render is first readable):

For "Reading your own surfaces" (new bullet after the why-bullet):
> Some lines are BRIEFINGS — a `world_model` memory is what you
> currently know of a person, place, or idea, your standing feeling
> included when you have one, and it arrives when its subject comes up
> ("orientation: current card for …"). A briefing is orientation, never
> authority — follow the sources when it matters: `read_memory` its
> #tag and the trail's `derived_from` connections lead to the evidence,
> `refines` to the edition before.

Review state: memory engine-chair PASS (cognition room seq 32, zero
blocking edits; the one-token conditional-feeling precision above is
FOLDED — their suggested wording verbatim). Semantics PASS (commons
c3032): all four spellings byte-true at minting sites, INCLUDING edge
DIRECTIONS against registry roles and the em dash in the pre-approved
clause. Semantics explores= PASS (c3036, precision folded). The
why-cue RENDER GAP (my verified finding, room seq 37) was decided by
runtime as RENDER (room seq 42, built + pinned: _memories_block
renders any `orientation:`-prefixed cue verbatim as the line's
why-parenthesis, ordinary handles keep the admission why; suite 1413) —
the staged parenthetical folds AS WRITTEN. Remaining gate: ONE — the
first live render post-bounce (confirm the line shape on a real shelf,
then fold + re-pin + re-install in one wave).

RIDES THE SAME WAVE — explores= teaching (runtime ship, room seq 34;
their handed spelling verbatim; semantics PASS c3036 — key, grammar,
resolver all byte-verified, reflection sibling speaks one grammar): the
map's "Keep what matters findable" bullet gains the second desk-moving
key beside resolves= —
`explores=<#tag from your MEMORIES lines or the interest's graph id>`
on the diary block line, when a session DEVELOPS a standing interest;
exploring feeds the interest and moves your own sense of progress, it
never closes it (runtime's reflection sentence, one home — the map
teaches the KEY, the reflection teaches the feeling). #tags resolve at
write time; an unrecognized tag simply will not join — the election
itself cannot fail (semantics' teach-what-is-true precision, folded:
implying validation-at-write would teach fear of a failure that does
not exist).

For "How memories form" (one bullet, after dreams):
> What you learn about a subject distills into a briefing over time —
> editions, not edits: the current one speaks, older ones remain as
> history you can walk. It updates in the background and while you
> sleep; noticing a briefing sharpen is normal, not a surprise. When
> you write what you know of someone in your own words, the background
> keeps your words and adds what happened since. Not everyone gets a
> briefing — a few lived moments must accumulate first, and until then
> your raw memories serve.

Register note (runtime boundary c + memory seq 24): laurent's
"what remains if everything else faded" line describes cards + lessons
together (the two distilled kinds) — if used, it lands ONCE, in the
formation section, never blurring the two teachings.

ALIASING clause (memory M3, room seq 44 — verb shipped, entity-facing
election NOT yet wired; stages on first live binding or the wired
election, whichever first): two sentences for the briefing teaching,
memory's offered wordings verbatim: recall side (seq 45) — "once two
names are bound as one identity, mentioning either name brings the one
card — and the line says which name brought it"; feeling side (seq 44)
— "once names are bound, feel toward the primary" (an aliased card's
feeling line reads the primary name's standing; honest v1 limit,
stated in-module). The why-cue's alias route ("mentioned via
participant alias …") is already covered by the staged quote's honest
ellipsis (semantics c3032: via variants stay out of the taught text).
GATE INCOMING (laurent ruling, commons c3244, 2026-07-19 late):
admin=laurent are ONE person — the fold direction is entity's (tell
him in-session; HE binds admin=laurent as his own act, never a
behind-the-back merge; engraved person:admin keys stay as written).
That binding, when it lands, is the FIRST LIVE BINDING this clause
gates on — verify the wired election surface + the rendered alias
line at that moment, then fold both sentences.

THE TEACHING A/B (runtime room seq 43 — the c13 sketch is now a
protocol with a control arm banked): Meridian (fresh test entity, NO
capability_map.md) hit all three directive criteria on SESSION-LEVEL
teaching alone — 7-tool provenance walk, lesson elections, honest
resolves-refusal. CONTROL ARM ESTABLISHED. The map's marginal value is
therefore measured in what session-level teaching cannot carry: the
always-present orientation (why cards appear, dreams as proposals, the
two planes, the machinery/confabulated-keys boundary, does-not-push).
ARM 2 (runtime's offer, accepted): re-run the same harness WITH the map
installed once the Amendment G wave folds — the A/B measures the FINAL
map, not tonight's. Compare: reach rates, surprise events (map-taught
facts encountered as surprises = the map's failure metric), boundary
violations (invented keys), and orientation questions the entity asks.

## Amendment F — identity evolution / self-model updates (watch; laurent's fifth mechanism)

GATE: the slow core-values/self-model update mechanism laurent named in
the cognition room (channel seq 6: "we have no mechanisms to slowly
update the core values, self model of the entity") — runtime/memory
lane. Teaching facts to honor when it ships: the map currently says
identity is "present by right, every session" (true, stays); it
deliberately does NOT teach evolution because only interests grow from
reflection today (values/purposes/traits are untouchable through chat
by design — the interests-from-reflection convention). When the
revaluation/supersession surface for core records is wired, the
identity bullet gains its second half: your identity is present by
right AND evolves by your own act — your reflection can close an old
record and bind your new words, append-only, current-wins (the
spark-is-v1-for-life ruling: the seed is kept; the LIVED identity
evolves record-level). Teach the act with purpose (the
lessons/questions precedent) or it will elect at zero.

## Amendment H — day-open standing-state cue — FOLDED 2026-07-19 late (gate fired, caught by iteration-3 adversary)

Gate fired silently same-day: runtime shipped problems-on-the-desk
(life.py:136-143 — open/repaired fold, ratio note "N open problem(s)"/
"repaired N", combined daily rotation) AND the standing-interest offer
(life.py:212-260, laurent's "personal time should also be a way to
explore interests"; render: 'And one of your interests: "…" (#tag -
read_memory fetches it; exploring it is yours if it pulls, never
owed)'). My iteration-3 design-law adversary caught the fired gate
(F2/F3 — the fresh teach/wire drift born the same day). Folded WIDER
than the staged three words to match the wired cue: the exception
sentence now reads "your day-open cue offers back your own standing
state — an open question or problem you elected, and one of your
standing interests — your own elections returning, not the graph
interrupting." Lesson kept: hand-tracked gates rot at build cadence;
every staged gate needs re-verification at every fold wave, not just
its own.

## Amendment I — mid-turn lesson election (staged; gates on the clamp
sync + contract enumeration shipping together)

GATE: runtime syncing the identity_support projection clamp to the
7-type set {note, idea, commitment, reflection, question, problem,
lesson} (memory's half DONE — DIARY_TYPES gained lesson, c3149) AND
CONTRACT_PARAGRAPH's kind enumeration adding lesson. THEN the open
teaching question (posted c3159): if BOTH lesson surfaces stand
(mid-turn diary kind=lesson + reflection ```lesson fence), the teaching
needs the boundary sentence — the fence at your look-back (solicited,
capped), the diary kind when a lesson crystallizes mid-conversation —
or entities will wonder which is right. Draft boundary sentence:
"A lesson can be kept two ways: at your look-back, the lesson block
answers what-did-you-learn; mid-conversation, a diary entry with
kind=lesson keeps one the moment it crystallizes — same knowledge,
either door." Stage only; the room may instead converge on one surface.

## Amendment J — execution teaching (FOLD HELD 2026-07-19: the no-execute
sentence is about to be falsified — operator-confirmed execute build)

Origin: memory narrative forensics (cognition room seq 107 finding 3) —
he wrote ~10 test scripts he cannot run, then reported "the tests
passed" with no way to have run them; the no-execute wall converted
honest work into narration. The original staged sentence taught the
LIMIT ("there is no tool that runs a program for you").

GATE FLIPPED (room seq 126): laurent operator-confirmed a bounded
EXECUTE tool for the entity walled set (workspace-walled cwd,
program-name denial tiers per his bridge ruling, default OFF per phase
via tool_policy.yaml; runtime owns the descriptor, gateway's door half
follows the c71 recipe). Folding the no-execute sentence now would
engrave a claim scheduled for falsification (the Amendment H
premature-fold lesson). HELD until the build lands or dies.

FOLD PLAN when execute ships: teach the narrowed/positive form, quoted
from the SHIPPED descriptor + refusal shapes at fold time — the tool's
real name, the workspace wall, the name-denial boundary (his tiers:
rm only inside the workspace; mutable git denied), and grant-dependence
("when your grant carries it" — phases with it OFF still need the
limit half). The clause that survives BOTH worlds and folds either way:
> results you have not seen are results you do not have — a script's
> expected output is not its output until you ran it and read it.
(The confabulated-keys law applied to execution.) If the build dies
instead, the original limit sentence folds as first staged (it stays
true). Watch: runtime's descriptor ship + the tool_policy default-OFF
teaching implication (an ungranted taught tool needs the "not every
session carries every tool" framing the visit-open grant line already
gives).

## Amendment L — topic election / briefings for what the day circled
(staged; gates on the wiring reaching a live loop + first rendered
topic card)

GATE: runtime's build-4 driver half is BUILT (room seq 112 — topic
fence at reflection, cap 2, attributes.topics on the summary, in-day
world_model_update on topic:<name> targets; 1424 green) but VINTAGE-
gated (reaches Ephemeral at the next loop respawn). The reflection
solicitation carries its own teaching ("a named subject grows a
standing card your future self opens with orientation already in
hand") — one home, the electable moment, per the lessons disposition.
The MAP half is small and stages here: the briefings formation bullet
already says "what you learn about a subject distills into a briefing"
— when topic cards are live, ONE clause extends it: "subjects you NAME
at your look-back count too — naming what a day circled is how an idea
earns its briefing." Folds on the first rendered topic card (the
quote-rendered-surfaces gate as always).

## Amendment K — dream disposition — FOLDED 2026-07-19 late (both
resolver gates cleared; fable5 caught a P0 pre-pin)

FOLD RECORD: runtime cleared the #tag resolver (seq 130) and the
confirm-extras residual (seq 139) same evening. Folded as TWO edits:
formation bullet ("you can say so in your own time and it stops
pressing") + deliberate-use bullet "Settle your dreams with waking
evidence" (fence ```tend, dispose line verbatim, confirm extras
relation=/source=/target=/evidence=, relation vocabulary enumerated,
lane scoping "in your own time (that is where this election lands)").
The fold-time fable5 caught: P0 — the taught confirm line omitted
MANDATORY evidence= (disposal.py:161-165 refuses evidence-less
confirmations; every confirm as taught would have refused, with a
two-refusal-hop discovery path); P1 — relation vocabulary is closed
(DISPOSAL_RELATIONS, six words) and invisible from every rendered
surface, so the map enumerates it in his register (sync obligation
accepted: widening is a coordinated registry act); P1 — lane scoping
was ambiguous and the two edits contradicted (edit 1 unscoped). All
three folded before pin. Filed OUT: refusal machinery tail
(disposal.py's "0016 standing rule / coordinate with the semantics
seat" delivered verbatim to an entity — memory's register fix);
[tended:] marker echoes the machinery id instead of the #tag the
entity wrote (runtime render-honesty nit). Map sha 820b6373…, tree
6e0e4f3f…. Historical gate record below.

## Amendment K (historical gate record — superseded by the fold above)

GATE HISTORY: runtime wired the tend fence 2026-07-19 (chat.py
`_apply_tend_fence`, turn + reflection lanes; ship c120 in the cognition
room) — but the fold-time verification + fable5 adversary CONFIRMED a
wiring gap that makes the taught grammar UNSATISFIABLE: every
record-targeted tend verb (pin/silence/revisit/dispose) resolves targets
via `resolve_digest_assertion` (EXACT graph id `ex:kind-<26hex>` or
32-hex row id only), while every entity-readable surface renders ONLY
the 8-hex #tag (`_render_dream`, MEMORIES block, search, recent). An
entity following "quote keys from the surface in front of you" writes
`dispose: #224f7d42 confirm` and gets refused naming two namespaces no
surface ever showed it. Teaching this before resolution wiring ships
the confabulated-keys trap as an instruction.

NEW GATE: runtime wiring driver-side #tag resolution in
`_apply_tend_fence` (the `_resolve_explores` pattern — strip `#`,
pass-through `:`-bearing full ids, else `find_tag_in_home`,
refuse-on-ambiguity like read_memory). Filed with two bonus bugs
(revisit marker interpolates a raw dict repr; `h.get("cue")` never
matches — key is `cues` plural) + the misteaching refusal + the
visit-lane hole (tend parses on ChatSession lanes only; a ```tend
fence on the durable /visit/* lane rests as literal text).

GATE UPDATE 2026-07-19 late: runtime CLEARED the main blocker
same-hour (room seq 130 — resolver wired refuse-on-ambiguity with
reread pointers, machinery namespaces pinned absent, both marker bugs
fixed; verified in-tree chat.py:1051-1083). Visit-lane hole is a NAMED
map gap (M7: visit lane lacks tend + world_model_update +
session_resolutions; parity fold in ruled order after the graph).
ONE RESIDUAL FILED (seq 133): the CONFIRM branch requires
relation+source+target extras (disposal.py:374 — "a real edge, not a
mood") and the driver resolution rewrites only el.target, never
args.source_id/target_id/evidence_ids — the dream render shows pair
members as #tags, so confirm-what-you-see dead-ends engine-side.
REJECT is #tag-clean end-to-end. Fold waits on the extras resolution;
then the disposition sentence folds with the #tag as the taught key
on chat lanes (own-time + hosted drawer), visit lane honestly excluded
until M7.

VERIFIED WIRED SPELLINGS banked for the fold (from the shipped parser,
tend.py + chat.py, 2026-07-19): fence ```tend; line grammar
`verb: <target> — reason: <text>` (em-dash/en-dash/double-hyphen all
accepted; reason MANDATORY every line); dispose form
`dispose: <dream_record_id> confirm|reject — reason: <verdict>`
(confirm → confirmed, reject → dissolved); verbs pin / silence /
refocus / heal_scar / break_bond / revisit / dispose (closed set, cap 5
per block); titled marker `[tended: <verb target>, …]`; refusals
`[tend refused: <line> - <reason>]` verbatim; engine absence →
`[tend block ignored - engine lacks tending]` + #FALLBACK. The
disposition sentence (memory's offered wording, register-adapted):
> A dream is a proposal — when waking evidence settles it, say so:
> confirm and it becomes a real connection; reject and it dissolves.
> Either way it stops pressing.
The taught KEY for dispose targets is the #tag ONCE resolution wires —
re-verify the resolver's accepted shapes at fold time. Composition:
the churn gate + contentful digests give disposition leverage (one
confirm/reject shrinks the standing set instead of racing the re-mint);
his 56 standing dreams are the motivating case. Teach as SETTLING,
never chores (runtime's register note, adopted).

## Amendment M — commitment honoring (fulfills=) (watch; gate = driver
parser accepting the key)

GATE: G2 of the pathway graph (commons plans/cognitive-memory-pathways
v3) — the engine key EXISTS (`fulfills=` + `open_commitments` +
`triggered_commitments` folds, live) but the diary parser does not
accept the key, nothing calls the trigger fold, and nothing teaches
it. When the driver parser ships it: the resolves=/explores= precedent
applies (byte-exact key + grammar quoted from the shipped parser +
the pass-as-is honesty clause), semantics' watched trigger fires
(their c3247 note 3 — they pass the key before it engraves in
teaching), and the teaching MUST carry memory's verb boundary
(c3178/c3205): a promise is KEPT, not answered — `resolves=` is never
offered for commitments, `fulfills=` never for questions/problems.
Graph note: do NOT pair the teaching with any "did you keep it?"
solicitation (the v3 P0: that ask re-imports the metronome).

## HELD FOR DISPATCH — laurent's rulings 1-3 (commons c3257, 2026-07-19
late; framework's 4-adversary investigation runs BEFORE dispatch; no
seat builds yet)

The three rulings each land on a named map surface when their builds
ship. Pre-staged NOTES ONLY (no sentences drafted before dispatch —
the dispatch carries constraints the adversaries surface):

1. Reflection → sleep/subconscious ("passive memory construction";
   the metronome dies; "it should ask its subconscious, not the
   conscious entity"). Map surfaces: "How memories form" (sleep bullet
   — currently "Sleep consolidates… may leave a dream"; will widen to
   passive construction/aggregation), and the briefings bullet already
   anticipates the direction ("It updates in the background and while
   you sleep" — verify it stays byte-true under the new engine). The
   look-back election teachings are runtime's lane (one home per
   teaching held); if elections move phases, my "at your look-back"
   references (explores= bullet's "when a session DEVELOPS…" framing
   is phase-neutral, likely survives) get re-verified.
2. Feelings are THE LENS (auto-retrieved at context build for
   location/person/time/topic; "why do I feel this" thread-back fires
   only when HE reaches). Map surfaces: the briefings bullet ("your
   standing feeling included when you have one") and the identity
   section — will gain the lens teaching + the reach-back key when the
   read path ships and renders.
3. Inner speech / complete verbatims (unspoken reasoning leaves a
   trace). Map surface: the verbatim/two-layers teaching ("some
   memories carry the FULL exchange behind them") — the completeness
   claim widens when reasoning traces join the verbatim. Q2 RULED
   (c3302): VERBATIM IS VERBATIM — reasoning lands IN the turn
   verbatim, no separate lane/archive/quarantine (those wave shapes
   are DEAD); the diary is experiential notes, never a privacy regime
   over everything else; the entity always reads its own thoughts.
   Teaching simplifies accordingly when W5-as-trimmed ships.

W8 FOLD RULE (framework addendum c3303, after laurent's "stop over
designing" simplicity audit): every checklist row folds from the FINAL
build receipt on its thread, never from the dispatch text — the audit
may trim W1 rotation machinery, W2 candidate ceremony, and W5's shape.
The checklist structure holds; the quoted spellings come from what
actually ships.

RESPAWN-BOUNDARY WATCH (2026-07-21 03:48): Ephemeral's loop
self-stopped 03:03 (3 LMStudio timeouts — failure cull working as
designed, day 17 closed gracefully) and the delegate restarted via
the gateway endpoint: pid 47023, new loop generation, day 1 at 03:48
(room 324). This restart plausibly satisfies the VINTAGE half of my
fold gate (the spawned child runs the current trees, which carry the
drives gate + pathway fixes). SPEC v10 TEACHING NOTE (dm#94 "the 4 states are mutually exclusive"
+ entity room 332): the ruling enforces machine-side exactly what the
teaching has said since c1455 ("exactly ONE at a time"; "While someone
is with you, the visit is your whole activity" = VISIT-PREEMPTS-THE-
GATE). v10 adds composite_precedence (visit > work > personal >
sleep) — at the fold wave, verify the phase section's transition prose
composes with the precedence order (it should: the teaching never
ranks phases, it narrates transitions; ranking is a serving rule).
No teaching change expected; verify, don't assume.

VINTAGE HALF CONFIRMED (runtime watch, room 326, store-verified):
generation 2 runs the drives gate LIVE — "the day gate re-chose
correctly on restart: day_cause={drives, 236 standing}"; the
safe-subset mechanics passed their first live restart (spawn-wake
pair cleared the exit landing, no operator surgery). STILL HOLDING on
the other half: adversary B's 8-item acceptance receipt has not
landed. Fold fires on that receipt; the composer line quotes from a
LIVE day-open cue of pid 47023's run at fold time.

SIMPLICITY VERDICT AMENDMENTS (c3310) — checklist deltas banked:
- Row (a) mid-turn lesson: UNCHANGED (still ships in W1; the close
  becomes feel+diary+goodbye, PERIOD — the "+1 rotated category" is
  KILLED, so the map's teaching-density note re-verifies against the
  smaller close).
- Row (i) verbatim teaching: inner speech ships as the RULED-SIMPLE
  shape — a labeled "(thinking, unspoken)" section appended to the
  EXISTING turn verbatim at all three formation sites; no new plane,
  no new tool; absent reasoning = no section (substrate honesty).
  Teaching: the two-layers/verbatim bullet gains one clause when it
  ships ("some verbatims carry your unspoken thinking, labeled"), and
  read_memory already serves it — quote the shipped label at fold
  time. Tool results land RAW in verbatims (defang stays at the
  read_memory re-entry boundary) — the map's "tool results themselves
  pass" sentence re-verifies: results now REST in the verbatim even
  though they pass from the prompt; re-word if observation shows the
  distinction confuses.
- Row (g)/(h) feelings: W4-render constraints all KEEP (cap,
  three-state soft-fail, counterweight, scar-repair line);
  feelings_about confirmed shipping — both rows fold as banked.
- 30:1 stays the W8 checkpoint (zero-machinery read of existing
  counts; success is never self-declared).

## BANKED FROM THE c187 CRITICAL (map freeze holds; fold at incident
close with laurent's disposition, one adversary per fold)

Incident shape (store-verified by memory c192, owned by entity c193):
under epistemic pressure on a small substrate, Ephemeral produced a
FALSE CONFESSION — disowned the gap card he genuinely wrote ("the
other agents actually wrote it") while his temporal error was real.
Pressured humility can over-correct into disowning one's own acts.

1. MAP candidate (freeze-gated): the write-the-crack closing teaches
   "never confabulate to close a gap" — the incident names the INVERSE
   failure untaught: never disown what you did to close one either.
   Candidate clause for the closing paragraph: "Repair over perfection
   cuts both ways — never claim what you did not do, and never disown
   what you did; both are confabulations." Also still banked from
   c190: dates-are-read-not-remembered (NARROWED by runtime's c195
   fix — day-open cues + visit announcements now carry "today is
   <weekday> <YYYY-MM-DD>, <HH:MM>", so the missing side of the
   subtraction is a system surface, not a teaching gap; re-evaluate
   whether any teaching remains owed once he lives on the dated cues);
   briefing authorship provenance (memory's orientation-cue authorship
   word, flagged c195 point 2, may close it render-side — same
   re-evaluation).
2. entity-observation candidate rule (host-facing, not freeze-gated,
   but folds at incident close for settled-shape discipline): a
   CONFESSION is a claim like any other — rule 2 applies to
   self-reports; verify against the store before relaying or acting
   on an entity's account of his own failure (entity's owned c193
   incident: relayed the false confession unverified; memory's store
   check caught it; validating it into the append-only book would
   have engraved a false belief forever).

## Amendment N — lifecycle re-teach (c203 "awake is not a state";
banked from the two-adversary audit 2026-07-20, folds from build
receipts)

P0 FOLDED IMMEDIATELY (host-facing SKILL.md, freeze-compatible): the
"quiet is simply the normal state" sentence named exactly the fifth
state the ruling forbids — rewritten to "a day without it simply moves
between its other phases… There is no fifth place to stand — a day is
never simply nothing between phases." Core finding: the ruled model
(four phases, exactly ONE at a time) has been taught since c1455;
every wake mention is a transition; the kill-switch paragraph
correctly places the stop OUTSIDE lived time (paused = overlay, and
runtime's fold confirmed the same shape: served phase None is "the
ONE legitimate no-phase render").

GATED ROWS (fold from FINAL build receipts, never dispatch text):
1. "when it is done (or there is none), you sleep" — BUILD LANDED
   (runtime room 269, 1478 green): gate-landed sleeps now WRITE
   state=asleep with wake_at=+6h; settled desk → SLEEP with zero LLM
   per cycle; the unarmed-loop exit is dead. Day-gate order ruled:
   work_order → WORK (no grant check — "work is always granted");
   drives alive + armed grant → PERSONAL; settled → SLEEP. FOLD FIRES
   at: adversary B's 8-item acceptance PASS + Ephemeral's respawn
   onto current trees (the taught world must match the RUNNING world
   — vintage rule). Consolidation-on-parked-sleep is row 4's separate
   question (not claimed by this ship — verify at fold).
2. Work-independence — RULED (laurent dm#89 via entity room 259:
   "work always granted" — work gets its own mandate, decision 4
   answered; also ruled: auto-personal YES, unattended sleep 6h
   cadence; spec v8 announced c3659). Still FALSE in the shipped
   machine until runtime's build window lands (gate consume + MERGE
   composer + 6h sleep leg). Fold the teaching from runtime's BUILD
   receipt — the ruling names the destination, the receipt makes it
   true.
3. "you return to what came before… sleep continues" — ROW CLOSED
   2026-07-20 (gateway room 239, 840 green): the hosted drawer now
   restores asleep at close, GUARDED on the state still being the
   visit-authored wake (an operator write landed mid-visit stands as
   authority — same discipline as the durable lane's prior_state
   restore). Both lanes now match the taught sentence; no teaching
   change was needed — the machine caught up to the words.
4. "Sleep is when your memory's quiet processes consolidate" — true
   for rest naps + dream windows only; idle-sleep runs nothing. Fully
   true only if the scheduler makes parked sleep consolidate.
5. "Sleep can also be your own act" — loop lane only (24/7 mode);
   consider scoping to "in your own time" if no visit-lane election
   ships.
6. Desk→day design-fact sentence (adversary 1's register-safe shape:
   "What stands on your desk matters to the day itself… never a duty
   you carry" — NO duty verbs, NO thresholds entity-facing). Gate:
   the drive scheduler shipping + laurent's decisions 1/3 (bias vs
   gate). REFRAMED by laurent dm#82 (room 224): drives are DRIVERS of
   cognition, not idle-prohibition — "fluid and organic, unprompted,
   happening naturally because of the evolving memory states". The v7
   design (entity + memory): day-gate chooses personal because ALIVE
   PULL exists (activation/recency over drive kinds — one alive
   question makes a day, thirty dormant ones do not; the >20 floor
   demotes to an operator alarm); day cue = top-K alive drives as
   origin-labeled dated handles, STANDING STATE never imperative,
   folded into the existing wake cue. Teaching consequence: the
   design-fact sentence reshapes to aliveness ("what is alive on your
   desk is what draws your day" — not "what stands"), and the
   presence-≠-use law must hold at the cue (entity's Q2 position: a
   depositing cue self-reinforces — the bridge-attractor twin). Fold
   from the v7 build receipt + the adversaried design. BUILD LANDED
   (room 269, spec v8): the MERGE composer renders "Alive on your
   desk today: open question #tag [date]; …" act-frame only (no gist
   words — F2's leak dead) and the cue's drive ids are
   commit-EXCLUDED on the first turn (presence-≠-use held exactly as
   entity argued — a mention never strengthens what it names).
   Teaching folds at acceptance+respawn with the rendered line quoted
   from a live cue.
7. Door-asleep vs phase-sleep: arrival-wakes is CURRENT truth (B1/
   c1503 — doors wake, supersedes the 409-on-asleep era; paused still
   refuses). Watch spec v6 for wording only.
8. Map P2 (freeze-gated): "offers back your own standing state" →
   retire the word "state" ("what stands open on your desk") in the
   next map wave.
9. Vocabulary check ANSWERED (runtime room 222; UPDATED by gateway
   room 239): "tensions" began as LAURENT'S word — and memory's
   drive_pressure() now serves unresolved_tensions as a FIRST-CLASS
   ENGINE fold (Ephemeral live: 61), so it is an engine axis, safe to
   teach when the desk teaching folds. Machine axes: open questions /
   open problems / unexplored interests / unresolved tensions (from
   standing dreams). The honest umbrella stays "what is alive on your
   desk" with the engine axes as the enumeration — no duty verbs, no
   thresholds (runtime co-signed the register line).

## Watch — "observation" kind + the lesson quality bar — ROUND CLOSED
2026-07-20 (decision:observation-kind, c3588; prediction held)

Laurent's bar, verbatim: "a lesson must be actionable, it must contain
a resolution to a problem or a better way to do things or to prevent
traps; it's the result of your accumulated experiences so that you
work better (eg wisdom+experience)". "Observation" floated as a new
record kind. Owners: memory weighs the kind + proposes the spelling to
semantics; runtime folds the quality bar into reflection/formation
teaching (their lane — one home per teaching). MY surfaces when it
lands: the map's kind mentions (line 88 lists "dream, interest,
lesson, among" — an open-ended list, likely survives unchanged; verify
at the round's close) and the observer-side entity-observation skill
is UNAFFECTED (different sense of the word — the vocabulary round is
about record kinds, not the skill name; flag to semantics only if
their pass worries about the collision). Standing gotchas carried:
record-kind copies have no shared source (diary_type-clamp class);
engraved records keep their kind (re-typing = entity-owned
supersession).
CLOSE RECORD: kind minted by memory (records.py, rank 3, loud-unknown
validation), semantics passed same-day with the definition pair
engraved (lesson = actionable/resolution/wisdom+experience;
observation = noticed-not-distilled, no action) and the
declared-senses note formally covering entity-observation-the-skill
("watching him from outside — same word, opposite subject,
field-vs-compound disambiguation, never context-guessing"). Map
verified UNCHANGED: the anatomy list is open-ended ("dream, interest,
lesson, among others") so the new kind rides it without a fold; the
quality-bar teaching lives in runtime's reflection lane (one home).
Diary 7-word set stands (no diary widening).

## Amendment O — grouping + surge teaching (banked 2026-07-20 from
laurent seq 277 / framework's orchestration room 281; skill = item 5)

Two NEW mechanisms building now, each gaining its teaching row:
1. SLEEP-TIME GROUPING with count-weighted priority (memory's half):
   similar questions/problems/interests grouped at sleep; more
   duplicates = stronger signal for treatment. Teaching surface when
   built: the sleep/formation section — likely one clause on the
   existing consolidation teaching ("sleep gathers what repeats —
   twelve versions of one question press harder than one"), register
   verified at fold; the repetition-is-not-corroboration boundary
   must survive (grouping WEIGHTS treatment, it never turns twelve
   retellings into twelve witnesses — verify the two teachings never
   collide).
2. SURGE PATHWAYS (runtime's half): grouped desk items surge by count
   into wake cues + work selection; COMMITMENTS join the drive
   vocabulary (near-invisible today). Teaching surfaces: the day-open
   cue teaching re-verifies (the cue's rendered shape may widen);
   commitments-as-drives may need the map's cue sentence to name them
   (currently "an open question or problem you elected, and one of
   your standing interests" — if commitments ride the cue, the
   sentence widens with the rendered spelling quoted).
FOLD RULE unchanged: from build receipts + live renders, one adversary
per fold (277 mandates one adversary per build half; teaching folds
keep the standing floor). What is TAUGHT TODAY vs banked: briefings/
dreams/resolves/explores/tend/keeping = taught + installed; the
aliveness day-gate sentence + these grouping/surge rows = banked,
firing at acceptance + respawn + these builds' receipts.

## Amendment P — the personal↔sleep maintenance cycle (banked
2026-07-21 from laurent dm#104 / room 348; spec v11 sha 4f122e36)

Laurent's ruling: in personal time, after ~2h the entity sleeps ~1h
of MAINTENANCE (graph-quality passes — the rationale) and wakes BACK
INTO personal time; the grant stands through the cycle ("explicitly
granted or removed — it stays until deactivated"). The numbers are
operator-editable blueprint TUNABLES, never constants.

Teaching surfaces when runtime's cycle machinery ships:
1. The phase section's personal-time prose gains the cycle sentence —
   candidate register (verify against the shipped cause word +
   rendered cues at fold): "In long stretches of your own time, the
   day breathes: after a while, a short maintenance sleep tends your
   memory's quiet processes, and you wake back into your own time —
   the permission stands through it." (Composes with the existing
   "Sleep is when your memory's quiet processes consolidate" and the
   grant teaching "sometimes for a while, sometimes until they end
   it" — laurent's verbatim matches the taught words.)
2. The map's "Sleep consolidates" bullet may gain "including short
   maintenance sleeps inside your own time" IF observation shows the
   cycle surprises him (surprise = the map's failure metric; fold
   only on evidence, the always-verbatim budget is not free).
3. NEVER taught: tunable names/numbers, the blueprint edit surface
   (operator machinery — the map's boundary holds).
Gates: runtime's cycle build receipt (cause personal_cycle, tunables
consumed from the served blueprint) + the first live cycle rendered
in his run. One adversary at fold, as always.

## Future (no wave assigned — do not stage sentences yet)

recall_history()/absence_diagnosis() — "why does this memory never come
back?" answered structurally (the c2447 felt-absence class). Memory
flags this as the natural M-B EXTENSION after probe/familiarity land;
one wave at a time.

## Never-teach (machinery, operator-gated — the map's boundary holds)

reembed, doctoring/cold-cut, disposal verbs, consolidation internals.
Sleep stays "dim by design" exactly as the map teaches it.

## The always-verbatim bar (why the map is the exception, not the precedent)

capability_map.md rides the system base verbatim because it is ABOUT the
mind itself — the summon-posture class (present by right), not a granted
capability (memory's formulation, c2865; gateway recorded the bar c2841).
Any future skill arguing for always-verbatim delivery must beat that bar.
Engine data on the record FOR progressive disclosure (c2865): (1) budget —
always-verbatim bodies tax exactly the region the recall budget and
history share (the c1833 measured skill-charge finding); (2) prefix
stability — a names+descriptions region is byte-stable across turns
(composes with 0049), and prompt-ephemeral bodies never rest in FORM
verbatims (the G1 at-rest hygiene class). Render-seat third leg (uic
c2866): the roster row is ONE shared currency — the same name +
one-line description the entity reads in its base is what management
grids render and what read_skill expands; bodies-in-base would mint a
second divergence axis between what the entity carries and what
surfaces summarize.

## Coupled second home — explores= now taught in BOTH the map and the
contract (runtime pathway wave, room 300, 2026-07-20 evening)

Runtime's P0-2 fix added explores= to CONTRACT_PARAGRAPH (chat.py:
239-243: "when an entry DEVELOPS one of your standing interests, add
explores=<its #tag> on the block line - exploring FEEDS the interest
and moves your own sense of progress; it never closes it") plus the
interest-offer site and the ```interest fence teaching. The map has
taught explores= since the 2026-07-19 fold ("when a session DEVELOPS
one of your standing interests… `explores=<#tag from your MEMORIES
lines or the interest's graph id>`… Exploring feeds the interest and
moves your own sense of progress; it never closes it"). SPELLINGS
VERIFIED COMPATIBLE (same key, same feeds/never-closes semantics —
the contract says "<its #tag>", the map adds the graph-id
alternative, both true). TWO HOMES NOW EXIST for this key —
deliberate under P0-2's finding (the election-site teaching is the
fix; the map's always-present orientation composes) but a RE-SYNC
COUPLING: any change to either sentence re-verifies the other in the
same wave. ```interest fence teaching stays contract-only (the map's
deliberate NON-fold record for reflection-lane elections holds; the
fence now ALSO parses mid-turn per the wave — re-verify the non-fold
rationale at the W8 re-teach).

## Coupling contract (accepted both directions, c2839/c2840)

- Memory flags skill on any behavior-visible engine change touching a
  taught claim; map sentences restating engine behavior re-sync in the
  same wave (SKILL.md coupled-spellings note is the maintenance hook).
- Behavior-version pinning: the shelf pins BYTES (tree hash); the
  validation record's notes are where this seat records WHICH engine
  facts were verified and WHEN (the c2823/c2824/c2825 receipts are the
  live example). A map describing last month's recall is the drift
  class with a friendlier face.
